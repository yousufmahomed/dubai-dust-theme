{%- comment -%}
  Default Page Template
{%- endcomment -%}

<div class="page-template">
  <div class="container container--narrow">
    <h1><span class="gold-gradient">{{ page.title }}</span></h1>
    <div class="glass-card" style="line-height: 1.9;">
      {{ page.content }}
    </div>
  </div>
</div>
